This ER diagram visualizes the schema for the Ratings & Communication Service. It consists of two primary entities: 'RatingReview', which captures feedback on vendors and riders, and 'SupportTicket', which manages user support requests. The diagram displays each entity with its attributes and primary keys. No relationships are drawn between these entities as they are logically separate within the provided schema.
