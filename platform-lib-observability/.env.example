# The name of the service, used for tagging all telemetry data (logs, metrics, traces)
# This is a mandatory field.
SERVICE_NAME=my-awesome-service

# The minimum log level to output.
# Options: 'fatal', 'error', 'warn', 'info', 'debug', 'trace'
# Default: 'info'
LOG_LEVEL=info

# Global switch to enable or disable distributed tracing.
# Default: true
TRACING_ENABLED=true

# The gRPC endpoint for the OpenTelemetry collector.
# The default port for OTLP gRPC is 4317.
# Default: http://localhost:4317
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# The sampling ratio for traces (a value between 0.0 and 1.0).
# 1.0 means sample 100% of traces (good for development).
# 0.1 means sample 10% of traces (a reasonable production starting point).
# Default: 1.0
TRACE_SAMPLER_RATIO=1.0