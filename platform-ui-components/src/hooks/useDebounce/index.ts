/**
 * Barrel file for the useDebounce hook.
 * This simplifies imports for consumers of the library, allowing them to import
 * from `@platform/ui-components/hooks` instead of a deeper path.
 */
export { useDebounce } from './useDebounce';