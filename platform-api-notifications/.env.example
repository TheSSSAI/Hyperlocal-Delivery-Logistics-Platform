# Application Configuration
NODE_ENV=development
PORT=3000
API_PREFIX=api

# Observability & Logging
LOG_LEVEL=debug

# AWS Configuration
AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID= # Recommended to use IAM Roles in production (IRSA)
AWS_SECRET_ACCESS_KEY= # Recommended to use IAM Roles in production (IRSA)
AWS_SQS_QUEUE_URL= # e.g., https://sqs.ap-south-1.amazonaws.com/123456789012/notification-events-queue

# Firebase Configuration (for FCM)
# Store the entire base64 encoded service account JSON here or use AWS Secrets Manager
FIREBASE_SERVICE_ACCOUNT_BASE64=

# Internal Service Dependencies
IDENTITY_SERVICE_BASE_URL=http://platform-api-identity.platform-services.svc.cluster.local

# Database (PostgreSQL for Templates)
# While the core service is stateless, this provides an example if template management is moved to DB.
# In the current spec (templates in config), these are not used but are good practice to include.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=admin
DB_DATABASE=notifications_db