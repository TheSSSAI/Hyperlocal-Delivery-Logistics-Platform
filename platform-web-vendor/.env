# -----------------------------------------------------------------------------
# Environment Variables for Vendor Web Dashboard (platform-web-vendor)
# -----------------------------------------------------------------------------
# This file contains environment-specific variables for local development.
# Do not commit sensitive credentials to this file. Use .env.local for overrides.

# The base URL of the backend API Gateway.
# REQ-1-106 mandates all client requests route through a central API Gateway.
# The vite.config.ts proxy will use this target for requests to /api.
VITE_API_BASE_URL=http://localhost:8000

# The URL for the real-time WebSocket service, also routed through the API Gateway.
# REQ-1-092 and REQ-1-106 require a secure WebSocket (WSS) connection via the gateway.
VITE_WEBSOCKET_URL=http://localhost:8001