# =================================================================
# == Example Environment Variables for platform-web-vendor
# =================================================================
# In development, you can create a .env.local file to override these.
# DO NOT COMMIT .env or .env.local files to version control.

# The base URL for the backend API Gateway.
# This is used by the API client to make all HTTP requests.
# Example: http://localhost:3000/api/v1
VITE_API_BASE_URL=https://api.your-hyperlocal-platform.com/api/v1

# The URL for the WebSocket server, typically exposed via the API Gateway.
# This is used by the WebSocket client for real-time notifications.
# Example: wss://api.your-hyperlocal-platform.com
VITE_WEBSOCKET_URL=wss://api.your-hyperlocal-platform.com

# Mapbox Public Access Token (if needed on the client)
# VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here